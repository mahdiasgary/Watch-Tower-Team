version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo
    ports:
      - "0.0.0.0:27017:27017"
    volumes:
      - watchtower-data:/data/db

volumes:
  watchtower-data:
